# cook your dish here
t=int(input())
for i in range(t):
    N=int(input())
    A=list(map(int,input().split()))
    s=set()
    for i in A:
       s.add(i)
    if(len(s)<=1):
       print("YES")
    elif(len(s)==2 and max(list(s))-min(list(s))<=1):
       print("YES")
    else:
        print("NO")
