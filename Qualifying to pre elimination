# cook your dish here
n=int(input())
for i in range(n):
    c=0
    m,k=map(int,input().split())
    l=list(map(int,input().split()))
    l.sort(reverse=True)
    for j in range(m):
        if(l[j]>=l[k-1]):
            c+=1
    print(c)
